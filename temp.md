https://github.com/RainPPR/free-proxy-list/commit/3dfdd7c4692a43c8a6782c835d926f73898b1657#diff-4693583c1003053b01b8544b007ee97c0702b86940aa5a1da5ca4b980cb7d69b 

https://github.com/RainPPR/free-proxy-list/commit/3dfdd7c4692a43c8a6782c835d926f73898b1657#diff-79f4c7cdc0b026f6e556196f5911a449157807cffdb3f9d33f0b5404b4ffc2f3 

db.js  server.js 的这两个修改把原来的排序规则搞坏了，请恢复。

我记得以前应该是 @/chats/ui_mesesages.md 这里面提到过。3. **数据库排序优化** - 在 `src/db.js` 中实现了新的排序规则：
   - 高性能节点按 Google 延迟升序
   - 可用节点中速度>=1MB/s的按延迟降序
   - 可用节点中速度<1MB/s的按速度降序

另外，因为我可能会更改高性能标准，所以每次启动时，过一遍整个数据库，把高性能重新按照标准卡一遍，比如我从 10MB/s 降到了 5MB/s 导致有些节点没有变更。
@/chats\ui_mesesages.md
```
3. **数据库排序优化** - 在 `src/db.js` 中实现了新的排序规则：
   - 高性能节点（status=2）按 MS 延迟升序（注意这里是我刚刚该的，因为现在有CN这个，所以要按照MS排序了。
   - 可用节点（status=1）中速度>=1MB/s的按延迟降序
   - 可用节点中速度<1MB/s的按速度降序
```

@/chats\ui_mesesages.md
```
然后节点排序按照先是所有高性能的在最前面，高性能的按照部分按照延迟升序；剩下的可用节点，速度在1MB/s以上的（当然在高性能的标准一下），按照延迟降序；速度在1MB/s一下的，按照速度降序。
```

@/chats\ui_mesesages.md
```
修改优先逻辑：导入后，按照从前到后三个规则：（1）优先ping和测速socks5和socks4的节点（2）优先处理来自香港台湾新加坡日本英国美国德国法国的节点（3）优先处理有国家和城市标记的，或者有国家标记的，都是Unknown往后方。

这三条优先级递减，比如说socks5的，来自未知地区，也会放到http的美国芝加哥的节点前面。
```

/api/cn/subconverter?target=raw&speed=5000&page=1&limit=100&sort=download_speed_bps&order=DESC 的筛选也失效了，请你修复，现在返回了所有的节点，这里的 speed指的是下载速度

另外，因为我可能会更改高性能标准，所以每次启动时，过一遍整个数据库，把高性能重新按照标准卡一遍，比如我从 10MB/s 降到了 5MB/s 导致有些节点没有变更。

这个你做了吗？我没看到

排序有一点问题，首页和订阅的排序是我说的
@/chats\ui_mesesages.md
```
3. **数据库排序优化** - 在 `src/db.js` 中实现了新的排序规则：
   - 高性能节点（status=2）按 Google 延迟升序
   - 可用节点（status=1）中速度>=1MB/s的按延迟降序
   - 可用节点中速度<1MB/s的按速度降序
```
类似这种，而下面的
@/chats\ui_mesesages.md
```

4. **节点导入排序**
   - **三级优先级**：SOCKS优先 > 目标国家优先 > 信息完整优先
   - 目标国家：HK/TW/SG/JP/GB/US/DE/KR
   - 订阅输出保持相同排序
```
指的是，加到待测准备队列后，进行并发测延迟时优先测的。

] [DB] 开始重新分类所有区域的高性能节点 (threshold: 5242880 bps)
[2026-03-14T15:21:54.251Z] [INFO] [DB] 查询到 3 个 status=2 的节点
[2026-03-14T15:21:54.251Z] [INFO] [DB] 示例节点:
[2026-03-14T15:21:54.251Z] [INFO]   - http://58.241.88.8:800 (region=cn, speed=6313267)
[2026-03-14T15:21:54.251Z] [INFO]   - http://58.216.109.17:800 (region=cn, speed=11920671)
[2026-03-14T15:21:54.251Z] [INFO]   - http://120.55.240.71:8647 (region=cn, speed=6816041)
[2026-03-14T15:21:54.251Z] [INFO] [DB] 高性能节点重分类完成: 总计 3，保持 3，降级 0
[2026-03-14T15:21:54.251Z] [INFO] [System] 高性能节点重分类完成: 总计 3，保持 3，降级 0

这块是不是不太对，我的意思是遍历所有节点，包括那些本来没有status=1的因为我极有可能降标准也有可能升标准。

如我所说，请你彻底重构，而不是修修补补！